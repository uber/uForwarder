package com.uber.data.kafka.clients.admin;

/**
 * VisibleForTesting is an annotation to mark a method is visible for testing.
 *
 * <p>We copy this from guava to limit the number of dependencies.
 */
public @interface VisibleForTesting {}
